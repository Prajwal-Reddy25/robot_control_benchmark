[tool.pytest.ini_options]
pythonpath = ["src/robot_control_benchmark"]
addopts = "-ra"
markers = [
  "integration: deterministic end-to-end benchmark tests",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

